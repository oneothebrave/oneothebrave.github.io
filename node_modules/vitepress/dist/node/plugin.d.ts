import { Plugin } from 'vite';
import { SiteConfig } from './config';
export declare function createVitePressPlugin(root: string, { configPath, alias, markdown, site, vueOptions }: SiteConfig, ssr?: boolean, pageToHashMap?: Record<string, string>): Plugin[];
